events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # Logging
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # Gzip compression
    gzip on;
    gzip_comp_level 6;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    server {
        listen 8080;
        server_name _;
        root /usr/share/nginx/html;
        index index.html;

        # Enable serving static files
        location / {
            try_files $uri $uri/ /index.html;
        }

        # Serve the React/Vite app under /check-up with SPA fallback
        # Ensures deep links like /check-up/report/... load the app
        location ^~ /check-up/ {
            try_files $uri $uri/ /check-up/index.html;
        }

        # Handle /check-up without trailing slash
        location = /check-up {
            try_files $uri $uri/ /check-up/index.html;
        }

        # Cache static assets
        location ~* \.(css|js|jpg|jpeg|png|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        # Security - deny access to hidden files
        location ~ /\. {
            deny all;
        }

        # Health check endpoint for Cloud Run
        location /health {
            access_log off;
            return 200 "healthy\n";
            add_header Content-Type text/plain;
        }
    }

    # Redirect the assessment subdomain to the /check-up page on www
    server {
        listen 8080;
        server_name assessment.goagentflow.com;
        return 301 https://www.goagentflow.com/check-up;
    }
}
